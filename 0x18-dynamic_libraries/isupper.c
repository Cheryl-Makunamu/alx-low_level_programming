#include "main.h"

/**
 * _isupper - looks for uppercase character
 * @c: the parameter checked
 * Return: if c is uppercase returns 1 , if not 0
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
