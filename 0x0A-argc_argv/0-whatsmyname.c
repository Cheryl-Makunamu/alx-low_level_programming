#include "main.h"
#include<stdio.h>
/**
 * main - check the code
 * @argc: no of arguments
 * @argv: array of the arguments
 * Return: Always Zero.
 */
int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", *argv);
	return (0);
}
